[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["src"]

[project]
name = "karger-experiment"
version = "0.0.1"

[tool.setuptools.packages.find]
where = ["src"]
